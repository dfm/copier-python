name: Tests

on:
  push:
    branches:
      - main
    tags:
      - "*"
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nox-session: ["tests", "compiled", "generated", "build", "lint"]

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Configure git
        run: |
          git config --global user.email "gh-actions"
          git config --global user.name "gh-actions"
          git config --global init.defaultBranch main
      - name: Configure Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"
      - name: Cache pip packages
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: pip-${{ matrix.nox-session }}-${{ hashFiles('**/requirements.txt') }}-${{ hashFiles('**/noxfile.py') }}
          restore-keys: |
            pip-${{ matrix.nox-session }}-
      - name: Cache nox session files
        uses: actions/cache@v3
        with:
          path: .nox
          key: nox-${{ matrix.nox-session }}-${{ hashFiles('noxfile.py') }}
          restore-keys: |
            nox-${{ matrix.nox-session }}-
      - name: Cache pre-commit environments
        if: ${{ matrix.nox-session == 'lint' || matrix.nox-session == 'generated' }}
        uses: actions/cache@v3
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ matrix.nox-session }}-${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: |
            pre-commit-${{ matrix.nox-session }}
      - name: Install dependencies
        run: |
          python -m pip install -U pip
          python -m pip install -U nox plumbum
      - name: Run tests
        run: python -m nox --non-interactive -s ${{ matrix.nox-session }}
